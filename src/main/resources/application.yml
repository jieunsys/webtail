server:
  port: 8070
  shutdown: graceful

spring:
  main:
    allow-bean-definition-overriding: true
  thymeleaf:
    prefix: classpath:/thymeleaf/
    suffix: .html
    cache: false
  lifecycle:
    timeout-per-shutdown-phase: 1s
  application:
    name: webtail




